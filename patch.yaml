spec:
      containers:
      - image: ifyoma/udagram-frontend:v1
        name: udagram-frontend
        imagePullPolicy: Always
        resources:
         requests: 
          cpu: "100m"
         limits:
          cpu: "500m"
        ports:
        - containerPort: 80